<UserControl
    x:Class="MoneyManager.UserControls.AccountListUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    DataContext="{Binding AccountListControl, Source={StaticResource Locator}}">

    <UserControl.Resources>
        <DataTemplate x:Key="AccountTemplate">
            <StackPanel Margin="0,0,0,12" HorizontalAlignment="Stretch"
                        MinWidth="300" Holding="AccountList_Holding">
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Uid="Delete" Text="Delete" />
                        <MenuFlyoutItem x:Uid="Edit" Text="Edit" />
                    </MenuFlyout>
                </FlyoutBase.AttachedFlyout>
                <Grid HorizontalAlignment="Stretch">
                    <TextBlock Style="{StaticResource MenuItemText}" Text="{Binding Name}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Style="{StaticResource MenuItemText}" Text="{Binding CurrentBalance}"
                               HorizontalAlignment="Right" TextAlignment="Right" Margin="0,0,5,0" />
                        <TextBlock Style="{StaticResource MenuItemText}" Text="{Binding Currency}"
                               HorizontalAlignment="Right" TextAlignment="Right" />
                    </StackPanel>
                </Grid>
                <TextBlock Style="{StaticResource MenuItemSmallText}" Text="{Binding Iban}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel x:Name="MainPanel">

        <interactivity:Interaction.Behaviors>
            <core:EventTriggerBehavior EventName="Loaded">
                <core:InvokeCommandAction Command="{Binding LoadAccountsCommand}" CommandParameter="{Binding ElementName=MainPanel}" />
            </core:EventTriggerBehavior>
        </interactivity:Interaction.Behaviors>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Total: " Style="{StaticResource MenuItemSubSmallText}" Margin="0,0,5,10" />
            <TextBlock Text="{Binding AccountViewModel.TotalBalance, Mode=TwoWay}" Style="{StaticResource MenuItemSubSmallText}" Margin="0,0,5,0" />
            <TextBlock Text="{Binding SettingViewModel.Currency}"
                       Style="{StaticResource MenuItemSubSmallText}" Margin="0,0,5,10" />
        </StackPanel>
        <ScrollViewer>
            <ListView ItemsSource="{Binding AccountViewModel.AllAccounts}"
                  ItemTemplate="{StaticResource AccountTemplate}"
                  HorizontalAlignment="Stretch">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </ScrollViewer>
    </StackPanel>
</UserControl>