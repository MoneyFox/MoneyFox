<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:moneyFox="clr-namespace:MoneyFox;assembly=MoneyFox"
             x:Class="MoneyFox.UserControls.BalanceView"
             BindingContext="{x:Static moneyFox:DesignTimeViewModelLocator.DesignTimeBalanceViewViewModel}">

  <ContentView.Content>

      <StackLayout HorizontalOptions="CenterAndExpand"
                   Orientation="Horizontal">
          <Label Text="{Binding TotalBalance}" />
          <Label Text=" / " />
          <Label Text="{Binding EndOfMonthBalance}" />
      </StackLayout>
  </ContentView.Content>
</ContentView>