<UserControl x:Class="MoneyFox.Uwp.Views.Settings.RegionalSettingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="using:Microsoft.Xaml.Interactions.Core"
             xmlns:i="using:Microsoft.Xaml.Interactivity"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400"
             DataContext="{Binding RegionalSettingsVm, Source={StaticResource Locator}}">

    <i:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding LoadAvailableCurrenciesCommand}" />
        </core:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <StackPanel>
        <AutoSuggestBox ItemsSource="{Binding AvailableCurrencies}" />
    </StackPanel>

</UserControl>
