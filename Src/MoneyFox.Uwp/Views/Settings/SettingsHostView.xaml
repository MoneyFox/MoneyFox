<views:BaseView x:Class="MoneyFox.Uwp.Views.Settings.SettingsHostView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
                xmlns:core="using:Microsoft.Xaml.Interactions.Core"
                xmlns:resources="using:MoneyFox.Application.Resources"
                xmlns:views="using:MoneyFox.Uwp.Views"
                xmlns:designTime="using:MoneyFox.Uwp.ViewModels.Settings"
                xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
                mc:Ignorable="d">

    <d:Page.DataContext>
        <designTime:DesignTimeWindowsSettingsViewModel />
    </d:Page.DataContext>

    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding InitializeCommand, Mode=OneWay}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <Grid Margin="{StaticResource MediumLeftRightMargin}" EntranceNavigationTransitionInfo.IsTargetElement="True" >
        <muxc:NavigationView x:Name="SettingsView"
                             PaneDisplayMode="Top"
                             IsBackButtonVisible="Collapsed"
                             IsSettingsVisible="False"
                             Loaded="OnLoad"
                             ItemInvoked="OnItemInvoked">
            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Tag="settings" Icon="Home" Content="{x:Bind resources:Strings.SettingsTitle}"/>
                <muxc:NavigationViewItem Tag="about" Icon="Home" Content="{x:Bind resources:Strings.AboutTitle}"/>
            </muxc:NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"
                   Padding="12,0,12,24"
                   IsTabStop="True" />
        </muxc:NavigationView>
    </Grid>
</views:BaseView>