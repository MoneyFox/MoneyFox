<UserControl
    x:Class="MoneyManager.Windows.Controls.StatisticControls.CategorySpreadingUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneyManager.Windows.Controls.StatisticControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windows="using:OxyPlot.Windows"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <TextBlock Text="{Binding Title}"
                   Style="{StaticResource DeemphasizedBodyTextBlockStyle}" />
        <windows:PlotView x:Name="SpreadingPlotView"
                          Model="{Binding SpreadingModel}"
                          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Margin="30" />
    </Grid>
</UserControl>
