<UserControl
    x:Class="MoneyManager.Windows.Views.UserControls.CashFlowUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windows="using:OxyPlot.Windows"
    DataContext="{Binding StatisticView, Source={StaticResource CoreModule}}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <TextBlock Text="{Binding Title}"
                   Style="{StaticResource DeemphasizedBodyTextBlockStyle}" />
        <windows:PlotView x:Name="CashFlowPlotView"
                          Model="{Binding CashFlowModel}"
                          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Margin="0,30,0,0" />
    </Grid>
</UserControl>