<Page x:Class="MoneyManager.Windows.Views.StatisticsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:basicControls="using:MoneyManager.Windows.Controls.BasicControls"
      xmlns:statisticControls="using:MoneyManager.Windows.Controls.StatisticControls"
      DataContext="{Binding StatisticView, Source={StaticResource CoreModule}}"
      mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Calendar"
                              x:Uid="SetDate"
                              Label="Set Date"
                              Click="SetDate" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Pivot Style="{StaticResource TabsStylePivotStyle}">
            <PivotItem>
                <PivotItem.Header>
                    <basicControls:TabHeader Label="Cash Flow"
                                             x:Uid="CashFlowTitle"
                                             Glyph="&#xE908;" />
                </PivotItem.Header>
                <Grid>
                    <statisticControls:CashFlowUserControl x:Name="CashFlowUserControl" />
                </Grid>
            </PivotItem>

            <PivotItem>
                <PivotItem.Header>
                    <basicControls:TabHeader Label="Spreading"
                                             x:Uid="SpreadingTitle"
                                             Glyph="&#xE774;" />
                </PivotItem.Header>
                <Grid>
                    <statisticControls:CategorySpreadingUserControl x:Name="CategorySpreadingUserControl" />
                </Grid>
            </PivotItem>

            <PivotItem>
                <PivotItem.Header>

                    <basicControls:TabHeader Label="Category Summary"
                                             x:Uid="CategorySummaryTitle"
                                             Glyph="&#xE8EC;" />
                </PivotItem.Header>
                <Grid>
                    <TextBlock Text="{Binding Title}"
                               Style="{StaticResource DeemphasizedBodyTextBlockStyle}" />
                    <statisticControls:CategorySummaryUserControl HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Stretch"
                                                                  Margin="-12,30,-12,0" />
                </Grid>
            </PivotItem>
        </Pivot>
        <CommandBar Grid.Row="1" ClosedDisplayMode="Minimal">
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Calendar"
                              x:Uid="SetDate"
                              Label="Set Date"
                              Click="SetDate" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Grid>
</Page>