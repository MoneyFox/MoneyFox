<views:MvxWindowsPage x:Class="MoneyFox.Windows.Views.CategoryListView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:userControls="using:MoneyFox.Windows.Views.UserControls"
                      xmlns:views="using:MvvmCross.Platforms.Uap.Views"
                      mc:Ignorable="d">

    <Grid EntranceNavigationTransitionInfo.IsTargetElement="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Pivot>
            <!--  Categories  -->
            <PivotItem >
                <PivotItem.Header>
                    <TextBlock x:Uid="CategoriesTitle"
                               Text="Categories"
                               Style="{StaticResource UwpPageTitleStyle}"
                               Margin="0,0,-10,0"/>
                </PivotItem.Header>
                <userControls:CategoryListUserControl Margin="{StaticResource NegativeMediumLeftRightWithTopMargin}" />
            </PivotItem>            
            
            <!--  Groups  -->
            <PivotItem >
                <PivotItem.Header>
                    <TextBlock x:Uid="CategoryGroupTitle"
                               Text="Groups"
                               Style="{StaticResource UwpPageTitleStyle}"
                               Margin="0,0,-10,0"/>
                </PivotItem.Header>
                <userControls:CategoryGroupListUserControl Margin="{StaticResource NegativeMediumLeftRightWithTopMargin}" />
            </PivotItem>
        </Pivot>

        <CommandBar Grid.Row="1" >
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Uid="AddCategoryViewModelLabel"
                              Command="{Binding CreateNewCategoryCommand}"
                              Icon="Add"
                              Label="New Category" />
            </CommandBar.PrimaryCommands>
        </CommandBar>

        <!--  Adaptive triggers  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="TitlePage.Margin"
                                Value="48,0,12,7" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</views:MvxWindowsPage>