<views:MyCategoryListView
    x:Class="MoneyFox.Windows.Views.CategoryListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:MoneyFox.Windows.Views.UserControls"
    xmlns:views="using:MoneyFox.Windows.Views"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid EntranceNavigationTransitionInfo.IsTargetElement="True"
          Margin="{StaticResource MediumLeftRightMargin}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="CategoriesTitle"
                   Style="{StaticResource UwpPageTitleStyle}" />

        <userControls:CategoryListUserControl x:Name="CategoryListControl" Grid.Row="1" Margin="{StaticResource NegativeMediumLeftRightWithTopMargin}" />

        <CommandBar Grid.Row="2"
                    Margin="{StaticResource NegativeMediumLeftRightMargin}" >
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="AddCategoryButton"
                              Icon="Add" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</views:MyCategoryListView>
