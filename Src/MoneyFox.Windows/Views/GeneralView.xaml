<views:MvxWindowsPage
    x:Class="MoneyFox.Windows.Views.GeneralView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MoneyFox.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:MoneyFox.Windows.Views.UserControls"
    xmlns:views="using:MvvmCross.Platforms.Uap.Views"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Stretch" Background="#EDEDED">
        <Pivot Margin="10, 10, 0, 0">
            <PivotItem>
                <PivotItem.Header>
                    <TextBlock Text="{Binding SettingsLabel}"
                               Margin="0,0,-10,0"/>
                </PivotItem.Header>
                <userControls:SettingsUserControl DataContext="{Binding SettingsViewModel}" />
            </PivotItem>

            <!--  Tiles  -->
            <PivotItem>
                <PivotItem.Header>
                    <TextBlock Text="{Binding AboutLabel}"
                               Margin="0,0,-10,0"/>
                </PivotItem.Header>
                <userControls:AboutUserControl DataContext="{Binding AboutViewModel}" />
            </PivotItem>
        </Pivot>
    </Grid>
</views:MvxWindowsPage>
