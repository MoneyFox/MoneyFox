<Page x:Class="MoneyFox.Windows.Views.RecurringPaymentListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:userControls1="using:MoneyFox.Windows.Views.UserControls"
      mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <userControls1:PageHeader>
            <userControls1:PageHeader.HeaderContent>
                <TextBlock Style="{ThemeResource PageTitleTextBlockStyle}"
                           Text="Accounts"
                           x:Uid="AccountTitle" />
            </userControls1:PageHeader.HeaderContent>
        </userControls1:PageHeader>

        <userControls1:RecurringPaymentListUserControl Grid.Row="1" />
    </Grid>
</Page>