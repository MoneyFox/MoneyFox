<xamForms:ReactiveContentView x:TypeArguments="viewModels:ModifyCategoryViewModel"
                              xmlns="http://xamarin.com/schemas/2014/forms"
                              xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                              xmlns:xamForms="clr-namespace:ReactiveUI.XamForms;assembly=ReactiveUI.XamForms"
                              xmlns:viewModels="clr-namespace:MoneyFox.ServiceLayer.ViewModels;assembly=MoneyFox.ServiceLayer"
                              x:Class="MoneyFox.Presentation.UserControls.ModifyCategoryUserControl">

    <ContentView.Content>
        <StackLayout Spacing="{StaticResource DefaultSpacing}" Margin="{StaticResource DefaultMargin}">

            <Label x:Name="CategoryNameLabel"
                   Text="{Binding Resources[CategoryNameLabel]}"
                   Style="{StaticResource DefaultTextStyle}" />
            <Entry Text="{Binding SelectedCategory.Name}" />

            <Label x:Name="NoteLabel" Text="{Binding Resources[NoteLabel]}"
                   Style="{StaticResource DefaultTextStyle}" />
            <Entry Text="{Binding SelectedCategory.Note}" />

        </StackLayout>
    </ContentView.Content>
</xamForms:ReactiveContentView>